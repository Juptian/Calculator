using System;
using System.Collections.Generic;
using System.Text;

namespace Calculator
{
    class JupIsDumb
    {
        //Turns out all this code is useless because it already splits it for me lol
        //I'm leaving this here to demonstrate how much of a dumbass I am lol
        //Juptian The Dumbass TM
        /*
        static double Add(string[] arr, int startIndex, int stopIndex)
        {
            double A;
            double B;
            string s = "";
            for (int z = startIndex; z < stopIndex; z++)
            {
                s += arr[z];
            }
            A = Convert.ToDouble(s);
            s = "";
            for (int z = stopIndex; z < arr.Length; z++)
            {
                switch (arr[z])
                {
                    case "+":
                    case "-":
                    case "*":
                    case "/":
                    case "^":
                    case "(":
                    case ")":
                    case "%":
                        for (int y = stopIndex; y <= z; y++)
                        {
                            s += arr[y];
                        }
                        lastJump = z;
                        break;
                }
            }
            B = Convert.ToDouble(s);
            Console.WriteLine($"{A} + {B}");
            return A + B;
        }
        static double Subtract(string[] arr, int startIndex, int stopIndex)
        {
            double A;
            double B;
            string s = "";
            for (int z = startIndex; z < stopIndex; z++)
            {
                s += arr[z];
            }
            A = Convert.ToDouble(s);
            s = "";
            for (int z = stopIndex; z < arr.Length; z++)
            {
                switch (arr[z])
                {
                    case "+":
                    case "-":
                    case "*":
                    case "/":
                    case "^":
                    case "(":
                    case ")":
                    case "%":
                        for (int y = stopIndex; y <= z; y++)
                        {
                            s += arr[y];
                        }
                        lastJump = z;
                        break;
                }
            }
            B = Convert.ToDouble(s);
            return A - B;
        }
        static double Mul(string[] arr, int startIndex, int stopIndex)
        {
            double A;
            double B;
            string s = "";
            for (int z = startIndex; z < stopIndex; z++)
            {
                s += arr[z];
            }
            A = Convert.ToDouble(s);
            s = "";
            for (int z = stopIndex; z < arr.Length; z++)
            {
                switch (arr[z])
                {
                    case "+":
                    case "-":
                    case "*":
                    case "/":
                    case "^":
                    case "(":
                    case ")":
                    case "%":
                        for (int y = stopIndex; y <= z; y++)
                        {
                            s += arr[y];
                        }
                        lastJump = z;
                        break;
                }
            }
            B = Convert.ToDouble(s);
            return A * B;
        }
        static double Div(string[] arr, int startIndex, int stopIndex)
        {
            double A;
            double B;
            string s = "";
            for (int z = startIndex; z < stopIndex; z++)
            {
                s += arr[z];
            }
            A = Convert.ToDouble(s);
            s = "";
            for (int z = stopIndex; z < arr.Length; z++)
            {
                switch (arr[z])
                {
                    case "+":
                    case "-":
                    case "*":
                    case "/":
                    case "^":
                    case "(":
                    case ")":
                    case "%":
                        for (int y = stopIndex; y <= z; y++)
                        {
                            s += arr[y];
                        }
                        lastJump = z;
                        break;
                }
            }
            B = Convert.ToDouble(s);
            return A / B;
        }
        static double Pow(string[] arr, int startIndex, int stopIndex)
        {
            double A;
            double B;
            string s = "";
            for (int z = startIndex; z < stopIndex; z++)
            {
                s += arr[z];
            }
            A = Convert.ToDouble(s);
            s = "";
            for (int z = stopIndex; z < arr.Length; z++)
            {
                switch (arr[z])
                {
                    case "+":
                    case "-":
                    case "*":
                    case "/":
                    case "^":
                    case "(":
                    case ")":
                    case "%":
                        for (int y = stopIndex; y <= z; y++)
                        {
                            s += arr[y];
                        }
                        lastJump = z;
                        break;
                }
            }
            B = Convert.ToDouble(s);
            return (double)MathF.Pow((float)A, (float)B);
        }
        static double Remain(string[] arr, int startIndex, int stopIndex)
        {
            double A;
            double B;
            string s = "";
            for (int z = startIndex; z < stopIndex; z++)
            {
                s += arr[z];
            }
            A = Convert.ToDouble(s);
            s = "";
            for (int z = stopIndex; z < arr.Length; z++)
            {
                switch (arr[z])
                {
                    case "+":
                    case "-":
                    case "*":
                    case "/":
                    case "^":
                    case "(":
                    case ")":
                    case "%":
                        for (int y = stopIndex; y <= z; y++)
                        {
                            s += arr[y];
                        }
                        lastJump = z;
                        break;
                }
            }
            B = Convert.ToDouble(s);
            return A % B;
        } */
    }
}
